<div class="box">
  <%= form_for :student_category, :url => {:action => 'categories'} do |f| %>
    <%#= error_messages_for 'student_category', :header_message => nil %>
    <div class="label-field-pair">
      <label class="inline">
        <%= t('enter_new_category') %> :
      </label>
      <div class="text-input-bg inline">
        <%= f.text_field :name, :value => @student_category.name %>
      </div>
    </div>
    <%= submit_tag "", :value => "► #{t('save')}", :class => "btn btn-pink btn-sm" %>
  <% end %>
</div>
