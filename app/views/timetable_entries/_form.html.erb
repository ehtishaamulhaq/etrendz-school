<%= form_for(@timetable_entry) do |f| %>
  <% if @timetable_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timetable_entry.errors.count, "error") %> prohibited this timetable_entry from being saved:</h2>

      <ul>
      <% @timetable_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :batch_id %><br>
    <%= f.number_field :batch_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
