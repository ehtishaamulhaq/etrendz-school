<div id="content-header">
  <h1>
    <%= t('fees_submission') %>
  </h1>
  <h3>
    <%= t('student_fees_submission') %>
  </h3>
  <div id="app-back-button">
  </div>
</div>
<div id="page-yield" style="padding:20px;">
  <div id="batchs1">
  <div class="row">
    <%= form_for :fees_submission_batch do %>
    <div class="row">
      <div class="label-field-pair">
      <div class="col-md-3">
        <label>
          <%= t('select_a_batch') %>
        </label>
        </div>
        <div class="col-md-3">
        <div class="text-input-bg">
          <%#= select :fees_submission, :batch_id, @batches.map { |c| [c.full_name, c.id] },{:prompt => "#{t('select_a_batch')}"}, {:onChange => "#{remote_function(:url => {:action => "update_fees_collection_dates"}, :with => "'batch_id='+value")}"} %>
          <%= select :fees_submission, :batch_id, @batches.map { |c| [c.full_name, c.id] },{:prompt => "#{t('select_a_batch')}"}, {:onChange => "onChangeRequest(this, '#{url_for(action: :update_fees_collection_dates, batch_id:'paramid')}')"} %>
        </div>
        </div>
      </div>
      </div><br>
      <div class="row">
      <div class="label-field-pair">
      <div class="col-md-3">
        <label>
          <%= t('select_fee_collection_date') %>
        </label>
        </div>
        <div class="col-md-3">
        <div class="text-input-bg">
          <div id="fees_collection_dates">
            <%= render :partial => "fees_collection_dates" %>
          </div>
        </div>
        </div>
      </div>
      </div>
    <% end %>
    <div id="student"></div>
    <div id="modal-box" style="display:none;"></div>
  </div>
  </div>
  <div class="extender"></div>
</div>
